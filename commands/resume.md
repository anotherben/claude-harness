Resume from where the last session left off. Do this silently and efficiently.

1. Read `MEMORY.md` — find the resume point
2. `git status --short` + `git log --oneline -5`
3. Check for any uncommitted changes and summarize them
4. State: "Resume point: [X] | Branch: [Y] | Clean: [yes/no] | Ready for: [next task]"

Then wait for instructions.
